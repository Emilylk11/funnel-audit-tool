# Checkout Champ API Credentials
# Set these in Vercel Dashboard > Settings > Environment Variables
# NEVER commit actual credentials to git

CC_LOGIN_ID=your-cc-api-login@email.com
CC_PASSWORD=your-cc-api-password
